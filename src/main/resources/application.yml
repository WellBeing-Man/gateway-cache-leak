spring:
  cloud:
    gateway:

      routes:
        - id: cache_request_body_route
          uri: no://route
          predicates:
            - Path=/**
#         with this memory leak occurs
#          filters:
#            - name: CacheRequestBody
#              args:
#                bodyClass: java.lang.String

#          this is no leak version,
          filters:
            - name: NoLeakCacheRequestBody
              args:
                bodyClass: java.lang.String